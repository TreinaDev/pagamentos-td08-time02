<div class='container'>
  <% if admin_signed_in? %>
    <%= link_to I18n.t(:approval_requisitions), admin_backoffice_pending_admins_path %>
    <h4><%= CreditLimit.model_name.human %>: <%= number_to_currency(CreditLimit.last.max_limit) if @credits.any? %></h4>
  <% end %>
</div>
